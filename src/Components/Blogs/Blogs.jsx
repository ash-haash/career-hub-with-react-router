import React from 'react';
import PropTypes from 'prop-types';

const Blogs = props => {
    return (
        <div>
            <h2>This is Blogs</h2>
        </div>
    );
};

Blogs.propTypes = {

};

export default Blogs;